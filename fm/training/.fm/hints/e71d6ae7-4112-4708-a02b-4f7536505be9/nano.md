Create a clean, flat-design educational infographic, 16:9 landscape, titled "DefaultStrategy vs MCMCStrategy" with the small subtitle "3D Gaussian Splatting densification". Use a light off-white background, a restrained palette of slate gray, warm orange for the left side, and teal-blue for the right side. Crisp sans-serif labels, generous white space, thin rounded outlines, no photorealism, no drop shadows.

Layout: a vertical dashed divider splits the canvas into two mirrored comparison columns. Eye flow goes top to bottom within each column, then across for the bottom summary bar.

LEFT COLUMN (warm orange), header label "DefaultStrategy" with a small subtitle "Heuristic 헤어리스틱":
- Top panel: a scattered cloud of orange ellipses of varied sizes over a faint image plane grid. A few ellipses are outlined with a bold ring and marked by a small upward arrow labeled "High screen gradient".
- Middle panel: three small icon steps arranged left to right with arrows between them, labeled "Duplicate if small", "Split if large", "Prune low opacity".
- Bottom panel: an upward-curving line chart with no ceiling line, arrow shooting off the top edge, labeled "Count grows freely".

RIGHT COLUMN (teal-blue), header label "MCMCStrategy" with a small subtitle "SGLD sampling 샘플링":
- Top panel: same style ellipse cloud in teal. Two faded, nearly transparent ellipses at the edge each have a curved dashed arrow flying toward a dense, solid cluster. Label "Relocate by opacity".
- Middle panel: one ellipse surrounded by small jitter arrows pointing outward in all directions, plus tiny dots suggesting random walk. Label "Inject noise 노이즈".
- Bottom panel: a rising line chart that flattens hard against a bold horizontal red-dashed ceiling line. Label the ceiling "cap_max".

BOTTOM BAR spanning the full width: a light rounded panel with three small chips reading "opacity_reg", "scale_reg", and "No prune needed", connected by a short arrow from the right column down into the bar. Add a tiny caption under the bar: "Regularizers replace pruning".

Keep every label to 5 words or fewer. Render all text sharply and legibly.
