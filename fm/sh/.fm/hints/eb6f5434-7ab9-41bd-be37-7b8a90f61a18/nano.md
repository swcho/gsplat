Educational infographic, title at top: "SH Projection vs Evaluation" with small Korean subtitle "사영 ↔ 평가". Clean flat vector design, white background, soft blue and orange accent palette, thin dark outlines, crisp sans-serif labels, 16:9 landscape aspect ratio.

Layout: a large circular loop diagram in the center, two small note cards along the bottom.

Center-left panel: a shaded sphere with a smooth colorful pattern on its surface (bright sky-blue top, dark brown bottom, a small bright sun spot), labeled "Function on sphere f(d)" and small Korean "구면 함수".

Center-right panel: a 4x4 grid of 16 small colored squares, some bright, some dim, labeled "16 coefficients c_k" and small Korean "계수".

Top arrow: a thick curved blue arrow from the sphere to the coefficient grid, labeled "Projection (analysis)" with a small formula tag "c_k = ∫ f · Y_k dΩ" and a tiny icon of an integral sign over a globe. Small caption under the arrow: "global, heavy, once".

Bottom arrow: a thick curved orange arrow from the coefficient grid back to the sphere, labeled "Evaluation (synthesis)" with a small formula tag "f(d) = Σ c_k · Y_k(d)" and a tiny icon of a sum sign over a single direction arrow. Small caption under the arrow: "local, cheap, per frame".

The two arrows form a circular cycle. Inside the loop, a tiny second sphere that is a blurred copy of the first one, labeled "L=3 approx" with a small "low-pass" tag, showing that going around the loop from the function side returns a smoother version.

Bottom-left note card: a small icon of a sharp spike becoming a smooth bump, labeled "Truncation → low-pass" and "Eval∘Proj ≠ identity".

Bottom-center note card: an icon of a grid returning to itself with a check mark, labeled "Proj∘Eval = identity" and "orthonormal basis".

Bottom-right note card: an icon of a camera, a red loss arrow going backwards into the coefficient grid, labeled "3DGS learns c_k by backprop" and "no projection needed".

Text must be short, legible, correctly spelled. No extra paragraphs.
